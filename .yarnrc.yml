# Public-repo Yarn config. Copied to the export root as `.yarnrc.yml`.
# node-modules linker so `tsc` / `tsdown` resolve dependencies conventionally
# (rolldown/tsdown is not PnP-friendly) in both CI and local builds.
nodeLinker: node-modules
enableTelemetry: false
